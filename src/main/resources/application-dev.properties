# Perfil de desenvolvimento

# Contexto para manter consistência com o WAR no WildFly
server.servlet.context-path=/gp

# Porta padrão (altere se necessário)
# server.port=8080

# Logs mais verbosos em dev
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Em dev, use as configurações padrão de banco (application.properties)
# Somente sobrescreva aqui se precisar apontar para outro banco
# Exemplo:
# spring.datasource.url=jdbc:mysql://localhost:3306/gppremium?useTimezone=true&serverTimezone=America/Sao_Paulo
# spring.datasource.username=monty
# spring.datasource.password=some_pass


# JWT: usa valores de application.properties; se precisar, defina um segredo de dev aqui
# gppremium.jwt.secret=devSecretExample